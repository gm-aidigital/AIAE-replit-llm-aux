<?xml version="1.0" encoding="UTF-8"?>
<!--
  Reusable outbound-observability module. Attach it to applications and
  external-services to obtain the common ExternalClientMetricsInterceptor and
  ExternalCallTimer metric schema. Application-owned Logbook configuration,
  inbound correlation filtering, logging profiles, Actuator exposure, and the
  Prometheus registry remain in application.

  Product usage analytics remains a distinct persistence-backed feature in
  event-logging-to-db-feature; operational telemetry and business analytics
  have different retention, consistency, and removal semantics.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>PACKAGE_REPLACE_ME</groupId>
        <artifactId>app-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>observability</artifactId>
    <name>Replit MVP — observability</name>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
